export interface CategoryDto {
  id: number;
  name: string;
  slug: string;
}

export interface LocationDto {
  id: number;
  cityName: string;
  state: string;
}

export interface SkillDto {
  id: number;
  name: string;
  slug: string;
}
